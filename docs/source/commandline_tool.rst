MatrixCtl as a pure commandline tool. You can use it as package, if you like,
but breaking changes may introduced, even in a minor change.

.. program-output:: python ../../src/matrixctl --help
   :shell:
